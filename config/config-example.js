'use strict';

/**
 * Minimal default config used in this repository checkout.
 *
 * The upstream server usually ships a much larger example config, but the
 * trainer/build tooling only needs a valid baseline object to merge against.
 */

exports.routes = {};
